-- 📌 Chargement de Rayfield UI
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

-- 📌 Vérification du chargement de Rayfield
if not Rayfield then
    warn("❌ Rayfield UI n'a pas pu être chargé !")
    return
end
print("✅ Rayfield UI chargé avec succès !")

-- 📌 Création de la fenêtre principale
local Window = Rayfield:CreateWindow({
    Name = "Rivals Aimbot",
    LoadingTitle = "Rivals Aimbot v1.0",
    LoadingSubtitle = "By azfasty",
    Theme = "Dark",  
    ConfigurationSaving = {Enabled = false}
})

-- 📌 Création de l'onglet Aimbot
local AimbotTab = Window:CreateTab("Aimbot", "crosshair") -- ✅ Onglet créé correctement
local AimbotSection = AimbotTab:CreateSection("Aimbot Settings") -- ✅ Section créée correctement

-- ✅ Correction du Toggle (Aimbot)
local AimbotToggle = AimbotSection:CreateToggle({
    Name = "Enable Aimbot",
    CurrentValue = false,
    Flag = "Aimbot",
    Callback = function(state)
        print("Aimbot activé :", state)
    end
})

-- ✅ Correction du Toggle (Draw FOV)
local FOVToggle = AimbotSection:CreateToggle({
    Name = "Draw FOV",
    CurrentValue = false,
    Flag = "DrawFOV",
    Callback = function(state)
        print("FOV activé :", state)
    end
})

-- ✅ Ajout d'un slider pour le FOV
local FOVSlider = AimbotSection:CreateSlider({
    Name = "FOV Size",
    Range = {50, 300},
    Increment = 5,
    CurrentValue = 100,
    Flag = "FOVSize",
    Callback = function(value)
        print("FOV Size :", value)
    end
})

-- ✅ Ajout d'un Color Picker pour le FOV
local FOVColorPicker = AimbotSection:CreateColorPicker({
    Name = "FOV Color",
    Color = Color3.fromRGB(255, 255, 255),
    Flag = "FOVColor",
    Callback = function(color)
        print("FOV Color changé :", color)
    end
})

-- 📌 Vérification finale
print("🔥 Menu Aimbot & ESP bien chargé dans Rayfield !")
