-- 📌 Chargement de Rayfield UI
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

if not Rayfield then
    warn("❌ Rayfield UI n'a pas pu être chargé !")
    return
end
print("✅ Rayfield UI chargé avec succès !")

-- 📌 Création de la fenêtre principale
local Window = Rayfield:CreateWindow({
    Name = "Rivals Aimbot",
    LoadingTitle = "Rivals Aimbot v1.0",
    LoadingSubtitle = "By azfasty",
    Theme = "Dark",
    ConfigurationSaving = {Enabled = false}
})

-- 📌 Création de l'onglet Aimbot
local AimbotTab = Window:CreateTab("Aimbot", "crosshair") 
local AimbotSection = AimbotTab:CreateSection("Aimbot Settings") 

local Toggle = AimbotTab:CreateToggle({
   Name = "Toggle Example",
   CurrentValue = false,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   -- The function that takes place when the toggle is pressed
   -- The variable (Value) is a boolean on whether the toggle is true or false
   end,
})

-- ✅ Toggle Draw FOV
local FOVToggle = AimbotSection:CreateToggle({
    Name = "Draw FOV",
    CurrentValue = false,
    Flag = "DrawFOV",
    Callback = function(state)
        print("FOV activé :", state)
    end
})

-- ✅ Slider pour régler la taille du FOV
local FOVSlider = AimbotSection:CreateSlider({
    Name = "FOV Size",
    Range = {50, 300},
    Increment = 5,
    CurrentValue = 100,
    Flag = "FOVSize",
    Callback = function(value)
        print("FOV Size :", value)
    end
})

-- ✅ Color Picker pour changer la couleur du FOV
local FOVColorPicker = AimbotSection:CreateColorPicker({
    Name = "FOV Color",
    Color = Color3.fromRGB(255, 255, 255),
    Flag = "FOVColor",
    Callback = function(color)
        print("FOV Color changé :", color)
    end
})

print("🔥 Aimbot & ESP chargés avec Rayfield !")
